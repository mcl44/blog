#配置端口
server:
  port: 8080

#配置mysql数据库
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/blog_study?useSSL=false&serverTimezone=GMT%2B8
    url: jdbc:mysql://42.192.227.166:3306/blog?useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: 94264944htz
#    password: 123456
    hikari:
      minimum-idle: 5
      # 空闲连接存活最大时间，默认600000（10分钟）
      idle-timeout: 180000
      # 连接池最大连接数，默认是10
      maximum-pool-size: 10
      # 此属性控制从池返回的连接的默认自动提交行为,默认值：true
      auto-commit: true
      # 连接池名称
      pool-name: MyHikariCP
      # 此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认1800000即30分钟
      max-lifetime: 1800000
      # 数据库连接超时时间,默认30秒，即30000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  #redis配置
  redis:
    host: 42.192.227.166
#    host: localhost
    port: 6379
    password: 94264944htz
  #mq配置
  rabbitmq:
    host: 42.192.227.166
#    host: localhost
    port: 5672
    username: guest
    password: guest
  #邮箱配置
  mail:
    host: smtp.qq.com
    # 邮箱号
    username: 1693740714@qq.com
    # 邮箱授权码
    password: qcahpeenlktscjdi
    default-encoding: UTF-8
    port: 587
    properties:
      mail:
      smtp:
      auth: true
      socketFactory:
      class: javax.net.ssl.SSLSocketFactory
  #图片大小限制
  servlet:
    multipart:
      max-file-size: 40MB
      max-request-size: 100MB

#配置MybatisPlus
mybatis-plus:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

#第三方配置信息
qq:
  app-id: "101948161"
  user-info-url: "https://graph.qq.com/user/get_user_info?openid={openid}&access_token={access_token}&oauth_consumer_key={oauth_consumer_key}"

aliyun:
  url: " https://blog-htz.oss-cn-hangzhou.aliyuncs.com"
  endpoint: "http://oss-cn-hangzhou.aliyuncs.com"
  accessKeyId: "LTAI4GCGuxX5QqHskUq4kf8Y"
  accessKeySecret: "DByRaVlmxDcg034CfcI0737ZIbdVPz"
  bucketName: "blog-htz"
